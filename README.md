### .dotfiles
Config files for mint xfce i3 rofi polybar stack

# .bashrc
    prerequisites :
    zoxide,
    starship,
    bash,
    bash-completion,
    tar,
    neovim,
    bat
# vimium-options.json
    for vimium plugin in browser
